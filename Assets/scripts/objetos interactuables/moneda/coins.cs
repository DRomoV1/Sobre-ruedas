using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class coins : MonoBehaviour
{
    /*public AudioClip audioMoneda;
    public AudioSource monedaAudido;
    [SerializeField] private GameObject efecto;
    [SerializeField] private float cantidadPuntos;
    [SerializeField] private puntajesMonedas puntajes;
    private void OnTriggerEnter2D(Collider2D other)
    {
        Debug.Log("Coger Monedas " + other.gameObject);
        if (other.CompareTag("Player"))
        {
            monedaAudido.PlayOneShot(audioMoneda, 1);
            Debug.Log ("Coger Monedas");
            puntajes.SumarPuntos(cantidadPuntos);
            Destroy(gameObject);
        }
        
    }
    */
}
